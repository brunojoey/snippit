import React from "react";
import { List } from "react-materialize";

function Feed() {
    return(
        /* for each snip, return as an ul/li */
        <List>
            
        </List>
    );
}

export default Feed;